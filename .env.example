# SIS Scraper Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# SUPABASE / DATABASE INTEGRATION (AISIS-compatible)
# =============================================================================
# Supabase project URL
# SUPABASE_URL=https://your-project.supabase.co

# Bearer token for data ingestion endpoint
# DATA_INGEST_TOKEN=your_ingest_token_here

# Batch size for Supabase uploads (default: 2000)
# SUPABASE_CLIENT_BATCH_SIZE=2000

# =============================================================================
# GOOGLE SHEETS INTEGRATION (AISIS-compatible)
# =============================================================================
# Provide service account JSON (as a single-line string) and spreadsheet ID
# GOOGLE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"your-project","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...@...iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}'
# SPREADSHEET_ID=your_spreadsheet_id_here

# =============================================================================
# SCRAPER CONFIGURATION (AISIS-compatible)
# =============================================================================
# Number of concurrent workers for PDF processing (default: 2)
# CURRICULUM_CONCURRENCY=2

# Delay in milliseconds between HTTP requests during discovery (default: 100)
# CURRICULUM_DELAY_MS=100

# Optional: Limit the number of PDFs processed (useful for testing)
# CURRICULUM_LIMIT=10

# Optional: Filter to specific program identifiers (comma-separated)
# CURRICULUM_SAMPLE=social-work,computer-science

# =============================================================================
# LEGACY / OPTIONAL SETTINGS
# =============================================================================
# SIS credentials (not currently used - PDF scraping doesn't require auth)
# SIS_USERNAME=your_username
# SIS_PASSWORD=your_password

# Term to scrape (optional, defaults to AY2024-Current)
# SIS_TERM=AY2024-Current

# Scrape mode: current, future, or all (optional, defaults to current)
# SIS_SCRAPE_MODE=current

# Number of concurrent requests for schedule sync (default: 5)
# SCHEDULE_SEND_CONCURRENCY=5

# =============================================================================
# BASELINE & REGRESSION DETECTION
# =============================================================================
# Percentage drop threshold for regression detection (default: 10)
# BASELINE_DROP_THRESHOLD=10

# If true, only warn about regressions without failing (default: false)
# BASELINE_WARN_ONLY=false

# =============================================================================
# DEBUGGING
# =============================================================================
# Enable debug logging (default: false)
# DEBUG_SCRAPER=false
